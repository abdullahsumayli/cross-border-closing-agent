# EO Score: Story 11 — التسعير الطبقي + Freemium · 2026-06-08

**BRD:** architecture/brd.md (AC-11.1 – AC-11.4)
**Composite:** 90/100 | **Decision:** Ship ✅

| Hat | Score | Notes |
|-----|:-----:|-------|
| Product | 10 | نموذج freemium كامل: باقة مجانية + 4 طبقات، ترقية فورية، فرض حدود برمجي (عقارات/منصات/استفسارات)، تتبّع تحويل + UI. يبني على Story 3. لا حواجز خارجية |
| Architecture | 9 | plan model نقي (حدود/فرض/تحويل)، يُدمج في routes الموجودة، صفر dependency، يعيد استخدام نمط posthog. متابعة: توحيد مفردات الطبقات (broker/developer من Story 3 ↔ free/starter/pro/agency) |
| Engineering | 10 | 7 اختبارات @AC-tagged، `tsc` + `next build` نظيف، صفر `any`، صفر أسرار، الفرض مربوط في route العقارات + route التوزيع |
| QA | 8 | المنطق النقي مُختبَر بالكامل؛ الفرض على مستوى route (403 عند تجاوز الحد) مربوط لكن غير مُختبَر بـ mock supabase. لا حاجز credential |
| UX | 8 | صفحة /plans بـ RTL + 4 طبقات + ترقية + ux-reference stub (L-006). لا browser render (L-002) |

**Composite: (10 + 9 + 10 + 8 + 8) × 2 = 90 ✅**

## مكتمل
- AC-11.1 باقة مجانية (عقار 1 / منصة 1 / 5 استفسارات، 0 ريال).
- AC-11.2 ترقية فورية عبر `PATCH /api/subscription/upgrade` → تحدّث الحدود (`planLimitsPayload`).
- AC-11.3 فرض برمجي: `canAddProperty` في إنشاء العقار، `canDistributeTo` في التوزيع، `canQualifyLead` متاح للتأهيل — `-1` = غير محدود (Agency).
- AC-11.4 `captureConversion` (free → paid) عبر posthog.
- DB: 010_plan_limits.sql (max_properties/max_channels/max_qualified_leads على subscriptions).

## متابعة (ليست blocker)
- توحيد مفردات الطبقات بين Story 3 (broker/developer) و Story 11 (free/starter/pro/agency).
- ربط أزرار الترقية المدفوعة بـ Stripe/Tap checkout (الحد يتحدّث الآن داخلياً؛ الفوترة الفعلية للطبقات الجديدة تحتاج price IDs).
- اختبار route-level للفرض (403) بـ mock supabase يرفع QA→9.

## Next
آخر قصة في النطاق. التالي: retro + تحديث الدروس.
