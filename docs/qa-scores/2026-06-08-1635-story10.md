# EO Score: Story 10 — محرك التوزيع العالمي + شبكة الموصّلات · 2026-06-08

**BRD:** architecture/brd.md (AC-10.1 – AC-10.4) · architecture/distribution-layer-spec.md
**Composite:** 88/100 | **Decision:** Ship code to main · المنصات الحية محجوبة على حسابات (L-001)

| Hat | Score | Notes |
|-----|:-----:|-------|
| Product | 9 | بنية الخندق مكتملة كوداً (feed + connectors + registry + engine + retry + status + sync) + UI. لكن كل الموصّلات mocks — لا منصة حية بعد، فالقيمة الواقعية «جاهزة معمارياً» لا «حيّة» |
| Architecture | 9 | الإسفين التنافسي: ChannelConnector واحد + registry للتوسّع (AC-10.4)، feed نقي، engine بـ resolver محقون + retry. موصّل جديد = صفر تعديل على الموجود |
| Engineering | 10 | 8 اختبارات @AC-tagged، `tsc` + `next build` نظيف، صفر `any`، صفر أسرار، XML escaping صحيح |
| QA | 8 | Cap L-001: لا منصة حية (Properstar/Juwai/…) — كلها mocks حتى تتوفّر الحسابات. المنطق (feed/registry/engine/retry/sync) مُختبَر بالكامل بـ mock connector |
| UX | 8 | صفحة /properties/[id]/distribute بـ RTL + اختيار منصات + حالة لكل قناة + ux-reference stub (L-006). لا browser render (L-002) |

**Composite: (9 + 9 + 10 + 8 + 8) × 2 = 88**

## مكتمل (code)
- AC-10.1 feed XML قانوني (Rightmove-ish، UTF-8، escaped، ≤50 صورة) + نشر بضغطة لقنوات مختارة.
- AC-10.2 حالة لكل قناة + retry (retryCount مُتتبَّع، فشل بعد استنفاد المحاولات، موصّل مفقود = failed لا throw).
- AC-10.3 update + remove يتزامنان عبر كل القنوات المربوطة.
- AC-10.4 registry: موصّل جديد لا يكسر الموجود — مُختبَر.
- DB: 009_distribution.sql (distribution_channels + distributions + leads.source_channel_id) + RLS.

## محجوب على إجراء بشري (L-001)
- حسابات المنصات الحية (Properstar/ListGlobally، TheMoveChannel، Rightmove Overseas، Juwai، JamesEdition، Bayut). كل واحدة = استبدال mock بموصّل حقيقي، صفر تعديل على engine (AC-10.4). + التأكد عند التسجيل أن المنصة تقبل مصدراً سعودياً (spec §6).

## Next
Story 11 (التسعير الطبقي + Freemium) — آخر قصة.
