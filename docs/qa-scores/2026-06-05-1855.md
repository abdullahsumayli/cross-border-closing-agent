# EO Score: Story 3 — التسعير والاشتراك · 2026-06-05

**Commit:** 019ad73
**BRD:** architecture/brd.md — Story 3 (AC-3.1 → AC-3.6)
**Composite:** 86/100  |  **Decision:** 🔧 Bridge Gaps (QA=7 < 8)

| Hat | Score | Notes |
|-----|:-----:|-------|
| Product | 10 | كل 6 ACs مُنفَّذة، SAR، عربي+إنجليزي، لا USD، Tap مؤجَّل صراحةً |
| Architecture | 9 | فصل نظيف (pure helpers + thin routes)، لكن لا تعليق scalability |
| Engineering | 9 | Elegance pause ✅، pure functions قابلة للاختبار، supabase.upsert بلا error check |
| QA | 7 | 19/19 unit tests تمر، لكن Stripe sandbox لم يُطلَق (بانتظار price IDs) ولا screenshots |
| UX | 8 | dir=rtl ✅، Cairo/Tajawal ✅، SAR ✅، loading state ✅، لكن browser لم يُفتَح فعلياً |

## Gaps (مُرتَّبة حسب الأولوية)

1. **QA — Stripe sandbox لم يُختبَر** (blocked على STRIPE_PRICE_*_ID): أنشئ المنتجَين في Stripe sandbox وشغّل `stripe trigger checkout.session.completed` محلياً
2. **Engineering/QA — `supabase.upsert()` في webhook بلا error check**: إذا فشل الـ upsert، يرجع 200 وStripe لا يعيد المحاولة → الاشتراك لا يُفعَّل
3. **UX — لا `aria-label` على أزرار الشرائح**: `tier-broker` و `tier-developer` يحتاجان `aria-label` للـ screen readers
4. **Engineering — npm audit**: 8 high في @supabase/ssr (موجودة قبل Story 3) — مؤجَّلة لـ Story 4

## Next action

Run /6-eo-bridge-gaps — يصلح QA (lowest hat) + Engineering gap الـ upsert
