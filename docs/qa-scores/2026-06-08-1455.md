# EO Score: Stories 1-4 — Verify + Re-ship (post-OpenRouter migration) · 2026-06-08

**Commit:** working tree (pre-ship; on top of a67d996)
**BRD:** 4-Architecture/brd.md (AC-1.1–1.10, AC-2.1–2.6, AC-3.1–3.6, AC-4.1–4.6)
**Composite:** 90/100 | **Decision:** Ship ✅

| Hat | Score | Notes |
|-----|:-----:|-------|
| Product | 10 | 28/28 ACs للشريحة الأساسية، الـ7 loops مربوطة، MENA-first، لا scope creep. **استعدنا مسار AI المكسور** في القصة 2 (كان معطّلاً على أي نشر نظيف) |
| Architecture | 9 | lib pattern متّسق (openrouter.ts ينضمّ لـ email/ghl/unifonic)، DI للاختبار، RLS، fire-and-forget + withRetry، health بـ service client (L-004). الـ env-var drift كان النقطة الوحيدة — أُصلح |
| Engineering | 10 | 206 اختبار يمر، `next build` نظيف، `tsc --noEmit` نظيف، regression test مضاف (@AC-2.5 ×3 + @AC-2.1)، صفر `any`، صفر أسرار |
| QA | 8 | Cap L-001: لا يمكن التحقق من نشر Coolify الحي ولا sandbox Stripe checkout من هذه البيئة؛ suite النشر الحي `describe.skip` عمداً. تغطية الوحدة/التكامل شاملة + البناء يثبت القابلية للنشر |
| UX | 8 | Cap L-002: لم يُفتَح المتصفح فعلياً هذه الجلسة. RTL عام (`lang=ar dir=rtl`)، viewport 375px (AC-1.7)، Cairo/Tajawal، ux-reference artifacts موجودة (onboarding/product-demo) → L-006 cap مرفوع، الصفحات تُبنى بنجاح |

**Composite: (10 + 9 + 10 + 8 + 8) × 2 = 90 ✅**

## ما تغيّر في هذه الجلسة (verify + reship — لا إعادة بناء)
1. **🔴 إصلاح regression:** `whatsapp/route.ts` كان يقرأ `ANTHROPIC_API_KEY` بينما `.env.example` يوثّق `OPENROUTER_API_KEY` → `Bearer undefined` → فشل كشف اللغة على كل نشر نظيف (AC-2.1). أُصلح + استُخرج إلى `src/lib/openrouter.ts` مع guard على المفتاح ومعالجة شكل الرد (يُرجع `unknown` → رسالة AC-2.5 بدل الانهيار الصامت).
2. **🟡 تضييق نوع:** `as any` → `as SupportedLanguage | null`.
3. **regression test:** `tests/story-2-language-detection.test.ts` (4 اختبارات: مفتاح مفقود / شكل رد خاطئ / فشل شبكة / مسار نجاح).

## Gaps (non-blocking — بيئة، ليست كود)
1. QA: التحقق من النشر الحي على Coolify + `/api/health` 200 على الرابط الحقيقي يحتاج خادمك (أو الرابط الحي) — خارج هذه البيئة.
2. UX: render بصري على 375px لم يُجرَ هذه الجلسة (L-002). البناء يُثبت التصريف لكن ليس المظهر.

## Decision
Composite 90 + كل hat ≥ 8 → **Ship** (لا حاجة لـ /6-eo-bridge-gaps). التالي: `/7-eo-ship`.
