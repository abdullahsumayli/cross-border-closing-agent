# EO Score: Story 9 — مولّد الإعلان بالـ AI متعدد اللغات · 2026-06-08

**BRD:** architecture/brd.md (AC-9.1 – AC-9.4)
**Composite:** 90/100 | **Decision:** Ship ✅

| Hat | Score | Notes |
|-----|:-----:|-------|
| Product | 10 | الميزة الأساسية مكتملة end-to-end: إدخال واحد → 5 إعلانات احترافية مُترجمة + تحرير/حفظ لكل لغة + UI. يعيد استخدام لغات Story 2 |
| Architecture | 9 | ad-generator دوال نقية + aiCall محقون، يعيد استخدام openrouter، RLS على 3 جداول جديدة (child عبر parent)، صفر dependency. generate route يطلق 5 نداءات LLM متوازية — مقبول للـ MVP |
| Engineering | 10 | 8 اختبارات @AC-tagged، `tsc` + `next build` نظيف، صفر `any`، صفر أسرار، parse fallback صلب |
| QA | 8 | Cap L-001/تكامل: توليد OpenRouter الحي + رفع البايتات لـ Supabase Storage غير مُمارَسَين في البيئة (محقونان). المنطق النقي مُختبَر بالكامل |
| UX | 8 | صفحة /properties/new بـ RTL + Cairo/Tajawal + role="alert" + ux-reference stub (L-006 cap مرفوع). لا browser render هذه الجلسة (L-002) |

**Composite: (10 + 9 + 10 + 8 + 8) × 2 = 90 ✅**

## مكتمل
- AC-9.1 توليد وصف احترافي من إدخال واحد · AC-9.2 خمس لغات بنبرة موحّدة · AC-9.3 تحرير + حفظ نسخة لكل لغة (`edited_by_broker`) · AC-9.4 جداول + RLS + مسار UTF-8 + ربط غير محدود.

## متابعة (integration half — ليست blocker)
- AC-9.4: واجهة سحب-إفلات للصور + bucket تخزين حي (الطبقة الخلفية + المسار + RLS جاهزة ومُختبَرة).
- توليد OpenRouter الحي يحتاج `OPENROUTER_API_KEY` على الخادم (موجود في .env.example).

## Next
Story 10 (محرك التوزيع + الموصّلات + XML feed).
