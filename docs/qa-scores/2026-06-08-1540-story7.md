# EO Score: Story 7 — Tap Payments (مسار خليجي ثانٍ) + PDPL · 2026-06-08

**BRD:** 4-Architecture/brd.md (AC-7.1 – AC-7.4)
**Composite:** 88/100 | **Decision:** Ship code to main · +2 محجوبة على حساب Tap حي (L-001)

| Hat | Score | Notes |
|-----|:-----:|-------|
| Product | 9 | AC-7.1 + AC-7.4 مكتملان كوداً ومُختبَران؛ AC-7.2 (منطق التوجيه ✅، لكن redirect لصفحة Tap محجوب) + AC-7.3 (runbook + فحص جاهزية ✅، التنفيذ على staging محجوب). قيمة حقيقية: بنية بوابتين متماثلة + تسوية + جاهزية PDPL |
| Architecture | 9 | mirror نظيف لنمط Stripe، دوال نقية (reconcile/route)، صفر dependency جديدة، HMAC timing-safe، الـ schema دعم `tap` مسبقاً |
| Engineering | 10 | 10 اختبارات @AC-tagged، `tsc` + `next build` نظيف، صفر `any`، صفر أسرار، `crypto.timingSafeEqual` |
| QA | 8 | Cap L-001: حساب Tap الحي + ترحيل staging محجوبان (موسومان صراحةً). الدوال النقية + مسارات توقيع الـ route مُختبَرة بالكامل |
| UX | 8 | لا UI جديد في القصة (بنية دفع)؛ AC-7.2 redirect محجوب. لا browser render |

**Composite: (9 + 9 + 10 + 8 + 8) × 2 = 88**

## ليش 88 وليس 90 (وليش bridge-gaps ما يرفعها)
النقطتان الناقصتان = **نصفا AC-7.2 (redirect لصفحة Tap) و AC-7.3 (تنفيذ الترحيل على staging)** — كلاهما محجوب على **مفاتيح/بنية خارجية** (حساب Tap حي + مشروع سحابي إماراتي). هذا cap L-001، مو نقص جودة كود. `/6-eo-bridge-gaps` يصلح أدنى hat بالكود — لكن الفجوة هنا credential مو code، فما يقدر يقفلها بصدق. القرار المتّسق مع توجيهك («ابني الكل ووسم الناقص + push لـ main»): أشحن الكود لـ main بعلامة صريحة «88 · +2 محجوبة على Tap».

## محجوب على إجراء بشري (L-001)
1. `TAP_SECRET_KEY` + `TAP_WEBHOOK_SECRET` من حساب Tap حقيقي → يقفل AC-7.2 redirect + يفعّل الـ webhook الحي (يرفع QA→9).
2. مشروع AWS me-central-1 / STC Cloud → تشغيل runbook الترحيل على staging يقفل AC-7.3.

## Next
Story 9 (مولّد الإعلان AI).
