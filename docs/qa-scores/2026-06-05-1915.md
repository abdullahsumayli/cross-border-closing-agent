# EO Score: Story 3 — التسعير والاشتراك · 2026-06-05 (re-score بعد bridge-gaps)

**Commit:** 0636bca
**BRD:** architecture/brd.md — Story 3 (AC-3.1 → AC-3.6)
**Composite:** 90/100  |  **Decision:** ✅ Ship

| Hat | Score | Notes |
|-----|:-----:|-------|
| Product | 10 | كل 6 ACs مُنفَّذة، SAR، عربي+إنجليزي، لا USD، Tap مؤجَّل صراحةً |
| Architecture | 9 | فصل نظيف (pure helpers + thin routes)، RLS على subscriptions، لا subagents (Q6=7) |
| Engineering | 10 | upsert error→500 ✅، elegance pause موثَّق، strict types، npm audit pre-existing Q8=7 لكن فورمولا 9.375×1.25→10 |
| QA | 8 | Q3 error paths: 9 ✅، Stripe sandbox مربوط بـ STRIPE_PRICE_IDs (بانتظار إنشائها)، لا screenshots |
| UX | 8 | aria-label+aria-pressed+role=alert ✅، cap=8 (artifact JSX لا HTML، browser لم يُفتَح) |

## Evidence

- Tests: 19/19 ✅ (`npm test tests/story-3-pricing-subscription.test.ts`)
- TypeScript: `tsc --noEmit` نظيف ✅
- Bridge-gap commit: `0636bca` — upsert error handling + accessibility

## Engineering — Elegance Pause ✅

**قبل:** upsert result مُهمَل — إذا فشل Supabase أعاد webhook 200 وStripe لا يُعيد المحاولة → الاشتراك لا يُفعَّل أبداً  
**بعد:** `const { error: upsertError } = await supabase.upsert(...)` → إذا فشل يُعيد 500 → Stripe يُعيد المحاولة تلقائياً

**الكود على هذه الحال أفضل ما يمكن في هذه القيود.** ✅

## QA — ما يتطلب إجراءاً بشرياً

1. **STRIPE_PRICE_BROKER_ID** + **STRIPE_PRICE_DEVELOPER_ID**: أنشئهما في Stripe Dashboard (sandbox)، أضفهما لـ `.env.local`
2. شغّل: `npm run dev` → `localhost:3000/pricing` → انقر "اشترك الآن" → تحقق من redirect لـ Stripe Checkout
3. شغّل: `stripe listen --forward-to localhost:3000/api/webhooks/stripe` + `stripe trigger checkout.session.completed`
4. تحقق من `subscriptions` table في Supabase dashboard: صف واحد بـ `status='active'`
5. تحقق من وصول إيصال البريد (Mailgun test mode)

## Gaps المتبقية (مُرجأة)

- npm audit: 8 high في `@supabase/ssr` (pre-existing) — **مؤجَّل Story 4**
- Stripe sandbox end-to-end: **بانتظار إنشاء price IDs** (إجراء بشري)
- Browser screenshot 375px: **إجراء بشري قبل v2**

## Next action

90/100 ✅ — شغّل `/7-eo-ship`
