# EO Score: Story 4 — النشر للإنتاج + فحص الصحة · 2026-06-05

**Commit:** f301986
**BRD:** architecture/brd.md — Story 4 (AC-4.1 → AC-4.6)
**Composite:** 90/100  |  **Decision:** ✅ Ship

| Hat | Score | Notes |
|-----|:-----:|-------|
| Product | 10 | loop:deploy + loop:observability مُغلقَين، PostHog events بـ SAR، لا scope creep |
| Architecture | 9 | posthog.ts نمط نظيف (single responsibility)، لا deps جديدة، Q6 subagents=7 |
| Engineering | 10 | posthog يعكس email.ts بالضبط، elegance pause ✅، Q8 npm audit=7 pre-existing |
| QA | 8 | 17/17 unit tests ✅، health endpoint محلياً ✅، AC-4.1+4.3+4.4 = human-action غير مُختبَرة |
| UX | 8 | لا تغيير UI — baseline مُحافَظ عليه، جميع أسئلة UX = N/A |

## Evidence

- Tests: 17/17 ✅ (`npm test tests/story-4-deploy-health.test.ts`)
- TypeScript: `tsc --noEmit` نظيف ✅
- Build: `npm run build` ← جميع Routes مُدرجة بما فيها `/api/sentry-test` ✅
- Health check: `curl http://localhost:3000/api/health` → `{"status":"ok","db":"pending"}` (pending لأن Supabase غير مُهيَّأ محلياً — صحيح)

## Engineering — Elegance Pause ✅

**posthog.ts:** يعكس email.ts بالضبط: `guard (if !apiKey return)` → `fetch(url, body)` → `.catch(()=>{})`. لا dependency جديدة، لا blocking، PostHog down ≠ feature down.

**instrumentation.ts:** 4 سطور فقط — لا `withSentryConfig` (يتجنب تعقيد build chain للـ MVP).

**الكود على هذه الحال أفضل ما يمكن في هذه القيود.** ✅

## QA — ما يتطلب إجراءً بشرياً

1. **AC-4.1:** انشر على ServerFast/Contabo واتبع `docs/deploy-runbook.md` خطوةً بخطوة
2. **AC-4.3:** أضف `NEXT_PUBLIC_SENTRY_DSN` لـ `.env.local`، ثم اضرب `/api/sentry-test` وتحقق من Sentry Dashboard
3. **AC-4.4:** أنشئ monitor على Better Stack أو Uptime Kuma → `https://app.crossborder.sa/api/health` كل 5 دقائق

## Next action

90/100 ✅ — شغّل `/7-eo-ship`
